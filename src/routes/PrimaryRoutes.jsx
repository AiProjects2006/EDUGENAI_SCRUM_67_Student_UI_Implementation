import React from 'react';
import { Routes, Route } from 'react-router-dom';

const PrimaryRoutes = () => {
  return (
    <Routes>
      {/* Configure your primary dashboard routes here */}
    </Routes>
  );
};

export default PrimaryRoutes;
