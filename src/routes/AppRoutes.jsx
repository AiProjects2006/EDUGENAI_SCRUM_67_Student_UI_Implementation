import React from 'react';
import { Routes, Route } from 'react-router-dom';

const AppRoutes = () => {
  return (
    <Routes>
      {/* Configure your top level routes here */}
    </Routes>
  );
};

export default AppRoutes;
